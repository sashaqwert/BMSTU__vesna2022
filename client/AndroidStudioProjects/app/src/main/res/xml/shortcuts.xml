<?xml version="1.0" encoding="utf-8"?>
<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:shortcutId="shortcut_animal_add"
        android:enabled="true"
        android:icon="@android:drawable/ic_input_add"
        android:shortcutShortLabel="@string/activity_animal_add"
        android:shortcutLongLabel="@string/activity_animal_add">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="ru.sccraft.bmstulabs.rip.animals"
            android:targetClass="ru.sccraft.bmstulabs.rip.animals.AnimalAddActivity" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the list determines what the user sees when
             they launch this shortcut. -->
        <categories android:name="android.shortcut.conversation" />
    </shortcut>
    <!-- Specify more shortcuts here. -->
</shortcuts>